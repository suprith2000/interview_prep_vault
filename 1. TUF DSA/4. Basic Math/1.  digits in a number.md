## 1. Given an integer N, return the number of digits in N.

### Brute Force Approach:

To count the number of digits in a number, we can use the algorithm created in [Extract Digits](https://takeuforward.org/maths/extract-digits-from-a-given-number) as now instead of extracting digits we are simply creating a counter to count the number of digits in the number..

**Algorithm:**

**Step 1:**Initialise a counter to store the number of digits.

**Step 2:**While N is greater than 0, execute the following:

1. Increment the counter by 1
2. Update N by removing its last digit by performing a modulo 10 (%10) operation on it.

**Step 3:**After exiting the while loop, we return the counter as the number of digits.

```
                                
import java.util.Scanner;

public class Main {
    public static int countDigits(int n) {
        int cnt = 0;
        while (n > 0) {
            cnt = cnt + 1;
            n = n / 10;
        }
        return cnt;
    }

    public static void main(String[] args) {
        int N = 329823;
        System.out.println("N: " + N);
        int digits = countDigits(N);
        System.out.println("Number of Digits in N: " + digits);
    }
}                          
```

### Optimal Approach

The logarithmic base 10 of a positive integers gives the number of digits in n. We add 1 to the result to ensure that the count is correct even for numbers that are powers of 10.

We cast the result to an integer to ensure that any fractional part is discarded giving the exact count of digits.

**Algorithm**

**Step 1:** Initialise a variable to store the count of digits of the number.

**Step 2:** The count of digits can be calculated using log10 N + 1.

1. log10 N operation gives the logarithmic base 10 of which returns the power to which 10 must be raised to, to be equal to N.
2. We add 1 to the result which accounts for the possibility that N itself is a power of 10.
3. Finally cast the result to an integer to ensure that it is rounded down to the nearest whole number.

**Step 3:** Return the value of count which represents the count of digits in the input integer N

```
                                
import java.util.*;

public class Main {
    static int countDigits(int n) {
        
        int cnt = (int) (Math.log10(n) + 1);

        // The expression (int)(Math.log10(n) + 1)
        // calculates the number of digits in 'n'
        // and casts it to an integer.

        // Adding 1 to the result accounts
        // for the case when 'n' is a power of 10,
        // ensuring that the count is correct.

        // Finally, the result is cast
        // to an integer to ensure it is rounded
        // down to the nearest whole number.

        // Return the count of digits in 'n'.
        return cnt;
    }

    public static void main(String[] args) {
        int N = 329823;
        System.out.println("N: " + N);
        int digits = countDigits(N);
        System.out.println("Number of Digits in N: " + digits);
    }
}             
```


## 2.  Given a number **n**. Count the number of digits in **n** which evenly divide **n**. Return an integer, total number of digits of n which divides n evenly.

**Note** :- Evenly divides means whether **n** is divisible by a digit i.e. leaves a remainder 0 when divided.  

**Examples :**

**Input:** n = 12
**Output:** 2
**Explanation:** 1, 2 when both divide 12 leaves remainder 0.

**Expected Time Complexity:** O(n)  
**Expected Space Complexity:** O(1)

```
class Solution{
    static int evenlyDivides(int N){
        // code here
        int x=N;
        int counter=0;
        while(x>0){
            int rem=x%10;
            if(rem!=0 && N%rem == 0){
                counter++;
            }
            x=x/10;
        }
        return counter;
    }
}
```

other approach :

```
public class Solution {
    public static int countDigits(int n){
        int N=n;
       ArrayList<Integer> lastDigits=new ArrayList<Integer>();
       while (N>0) {
           Integer e=N%10;
           if(e!=0){
               lastDigits.add(e);
           }
           N=N/10;
       }
       int count=0;
       for (Integer integer : lastDigits) {
           if (n%integer==0 && integer!=0) {
               count++;
           }
       }
       return count;
    }
}
```
